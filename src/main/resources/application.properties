# RapidAPI credentials
rapidapi.key=2505eda46amshc60713983b5e807p1da25ajsn36febcbf4a71
rapidapi.host=covid-19-statistics.p.rapidapi.com

# MySQL DB connection
spring.datasource.url=jdbc:mysql://localhost:3306/covid_tracker_db
spring.datasource.username=root
spring.datasource.password=Soynoob9182
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Habilitar scheduling
spring.main.allow-circular-references=true

# Configurar CORS (para el frontend)
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST

# Logging para debugging
logging.level.umg.com.umg=DEBUG

# Mostrar trazas completas de error
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Configuración de ejecución programada (opcional)
app.data-loader.enabled=true
# 5 segundos de espera inicial
app.data-loader.initial-delay=5000
